import canUseDom from '../../../utils/can-use-dom';
import { module, test } from 'qunit';

module('Unit | Utility | can use dom', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    var result = canUseDom;

    assert.ok(result);
  });
});
